# Language bindings (Phase 2)

Python and Lua bindings are planned for a later phase. They will be built on
the stable C-ABI surface exported by `libs/cabi` (see the project constitution,
Principle II). No binding code ships in Phase 1.
